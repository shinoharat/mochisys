<%= form_with(model: condition_detail, local: true) do |form| %>
  <% if condition_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(condition_detail.errors.count, "error") %> prohibited this condition_detail from being saved:</h2>

      <ul>
      <% condition_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :color_code %>
    <%= form.text_field :color_code %>
  </div>

  <div class="field">
    <%= form.label :tags %>
    <%= form.text_field :tags %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
